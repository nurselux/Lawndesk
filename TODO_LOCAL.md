# TODO — Local Environment Setup

All items complete as of 2026-04-01. No pending migrations or environment variables.

| Item | Status |
|------|--------|
| `CRON_SECRET` on Vercel | ✅ Done |
| Migration — `twilio_number_sid` on profiles | ✅ Done |
| Migration — `address` on booking_requests | ✅ Done |
| Migration — `estimates` table (created then dropped, consolidated into booking_requests) | ✅ Done |
| Migration — `scheduled_date`, `scheduled_time` on booking_requests | ✅ Done |
| Migration — `deleted_at` on booking_requests (soft delete) | ✅ Done |
